ungrouped:
  hosts:
    192.168.1.202:
      ansible_user: jaqb
      #ansible_ssh_private_key_file: "{{ ssh_private_key }}"
      ansible_password: $LSRV01PASSWD
  children:
    runners:
      hosts:
        192.168.1.204:
          ansible_user: piotr
          ansible_password:  "{{ lookup('env', 'LSRV01PASSWD') }}"
    homeassistant:
      hosts:
        192.168.1.201:
          
